{
  "name": "grs-purchase-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android:sync": "cap sync android",
    "android:build": "cd android && gradlew.bat assembleDebug",
    "android:apk": "cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
